# Regex

No spaces
```regex
^[^ ]*$
```

No spaces, no new lines
```regex
^[^\n ]*$
```

Only uppercase 
```regex
^[A-Z]+$
```

Product code only uppercase hyphen and underscores
```regex
^[A-Z-_]+$
```










